# Modern browsers only for reduced bundle size
# Reduces JavaScript polyfills and CSS prefixes

# Production environments
> 0.5%
last 2 versions
not dead
not op_mini all

# Explicitly target modern browsers
Chrome >= 90
Firefox >= 88
Safari >= 14
Edge >= 90

# Exclude legacy browsers to reduce bundle size
not IE 11
not IE_Mob 11
